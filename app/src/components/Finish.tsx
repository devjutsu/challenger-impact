import { LostButton } from "./LostButton"
import { WinButton } from "./WinButton"

export const Finish = () => {
    return (
        <div className=''>
            <WinButton /> <LostButton />
        </div>)
}